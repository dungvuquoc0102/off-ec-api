function isObject(value) {
  return Object.prototype.toString.call(value) === "[object Object";
}

function isObject(value) {
    static isArray() {
      console.log("hello");
    }
}

isObject.isArray();
